#include<stdio.h>
#include<stdlib.h>

void main()
{
	FILE *fp;
	if(fp = fopen("F:\\a 随时删除\\Hell.txt","w")) //判断文件是否打开成功（打开失败返回值为0）
	{
		fputs("Hello World!",fp);
		printf("运行成功！\n");
		system("pause");
	}
	else
	{
		printf("文件打开失败！\n");
	}
	fclose(fp); //记得脱钩
}
